<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
    >
    <item
        android:id="@+id/anim_form_right"
        android:orderInCategory="50"
        android:title="Translate from right"
        app:showAsAction="never"
        />

    <item
        android:id="@+id/anim_form_left"
        android:orderInCategory="50"
        android:title="Translate from left"
        app:showAsAction="never"
        />

    <item
        android:id="@+id/anim_form_bottom"
        android:orderInCategory="50"
        android:title="Translate from bottom"
        app:showAsAction="never"
        />
    <item
        android:id="@+id/anim_scale"
        android:orderInCategory="50"
        android:title="Scale"
        app:showAsAction="never"
        />

    <item
        android:id="@+id/anim_alpha"
        android:orderInCategory="50"
        android:title="Alpha"
        app:showAsAction="never"
        />
</menu>
